;;; keybindings.el --- setup keybindings (suggestions, guides, comfort) -*- lexical-binding: t; -*-

;;; Code:
(use-package which-key
  :config (which-key-mode))

(provide 'keybindings)
;;; keybindings.el ends here
